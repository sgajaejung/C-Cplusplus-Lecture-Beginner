
void func(sx, sy)
{
	if (selrgb == getpixel(sx,sy))
	{
		setpixel(x,y)

		func(sx, sy)
		func(sx, sy)
		func(sx, sy)
		func(sx, sy)
	}
}


selrgb
x,y = sx, sy
array.push( x, y )

while (!array.empty())
{
	x,y = array.pop
	if (selrgb == getpixel(x,y))
	{
		setpixel(x,y,rgb)
		array.push( x, y)
		array.push( x, y)
		array.push( x, y)
		array.push( x, y)
	}
}



text
pen
	- 여러개의 펜을 관리하기.
	- 다른색 펜으로 선을 그려보기.

brush
	- 여러개의 브러쉬 생성하기.
	- 다른색 브러쉬로 박스를 그려보기.

timer
	- 타이머 여러개 만들기.
	- 시계 만들기. 1,3,5 초마다 움직이는 초침.

graphic, image
	- 여러개 만들어서 출력하기
	- 키보드로 움직여보기
	- 애니메이션 시키기.


